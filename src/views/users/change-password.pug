extends ../base

block content
    if errorMessage
        span=errorMessage 
    form(method="post")
        input(name="oldPassword",placeholder="Old Password", type="password", required)
        input(name="newPassword",placeholder="New Password", type="password", required)
        input(name="newPassword2",placeholder="New Password Confirmation", type="password", required)
        input(value="Change Password", type="submit")