mixin video(video)
    a(href=`/videos/${video.id}`).video-mixin
        img.video-mixin__thumb(src=video.thumbUrl crossorigin="anonymous" alt="Video Thumbnail")
        //- div.video-mixin__thumb(style=`background-image:url(${video.thumbUrl}); background-size:cover; background-position:center`, crossorigin="anonymous")
        div.video-mixin__data
            span.video-mixin__title=video.title
            div.video-mixin__meta
                span #{video.owner.name} • 
                span #{video.meta.views} #{video.meta.views === 0 || video.meta.views === 1  ? "view" : "views" }
        //- h4
        //-     a(href=`/videos/${video.id}`)=video.title
            //- a(href="/videos/" + video.id)=video.title
        //- p=video.description
        //- ul
        //-     each hashtag in video.hashtags
        //-         li=hashtag
        //- small=video.createdAt
        //- hr


        //- #{video.views} #{video.views === 1 ? "view" : "views" }